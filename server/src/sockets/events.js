/**
 * Socket.IO Event Name Constants
 * Single source of truth — import these on both backend and frontend.
 */
const EVENTS = {
  MONEY_RECEIVED: 'money_received',
  CONNECT: 'connect',
  DISCONNECT: 'disconnect',
}

module.exports = EVENTS
